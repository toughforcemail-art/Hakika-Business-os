import { ProductDetail } from "@/components/ProductDetail";

export default function RealEstateProduct() {
  return <ProductDetail eyebrow="Hakika Real Estate" title="Make every property decision clearer." description="Manage properties, leases, tenants, collections and service work from a connected operational view." points={["Property and lease operations", "Tenant communication and collections", "Billing, payments and audit"]} />;
}
