import { ProductDetail } from "@/components/ProductDetail";

export default function ToughforceProduct() {
  return <ProductDetail eyebrow="ToughForce Security" title="Turn security operations into a shared view." description="Connect guards, sites, rosters, incidents and client accountability without losing the detail that keeps teams safe." points={["Sites, guards and rosters", "Incident and handover records", "Client-ready operational reporting"]} />;
}
