import { ProductDetail } from "@/components/ProductDetail";

export default function FinanceProduct() {
  return <ProductDetail eyebrow="Finance" title="See the numbers behind every move." description="Keep ledgers, cash, invoices and operational decisions aligned with clear controls and an audit trail." points={["Ledger and cash visibility", "Invoices and payment allocation", "Reports for confident decisions"]} />;
}
