import { AppShell } from "@/components/AppShell"; import { WorkspaceDashboard } from "@/components/WorkspaceDashboard"; import { requireCurrentApplication } from "@/lib/auth/applications"; export default async function Finance(){await requireCurrentApplication("FINANCE"); return <AppShell app="Finance" logo="/brands/finance/fallback.svg" alt="Finance logo pending supplied brand asset" groups={[{label:'Overview',links:['Dashboard']},{label:'Accounting',links:['Chart of accounts','Ledger','Journals']},{label:'Money',links:['Bank & Cash','Invoices','Reconciliation']}] }><WorkspaceDashboard app="Finance" eyebrow="Finance overview"/></AppShell>}
