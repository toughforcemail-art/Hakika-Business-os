import { AppShell } from "@/components/AppShell"; import { WorkspaceDashboard } from "@/components/WorkspaceDashboard"; import { requireCurrentApplication } from "@/lib/auth/applications"; export default async function ToughForce(){await requireCurrentApplication("TOUGHFORCE"); return <AppShell app="ToughForce Security" logo="/brands/toughforce/logo.jpg" alt="ToughForce Security logo" groups={[{label:'Overview',links:['Dashboard']},{label:'Workforce',links:['Guards','Rosters','Attendance']},{label:'Sites & Safety',links:['Clients & Sites','Incidents','Patrols']}] }><WorkspaceDashboard app="ToughForce Security" eyebrow="Security operations"/></AppShell>}
