import Link from "next/link";
import { AdminShell } from "@/components/AdminPage";
import { requireApplicationContext } from "@/lib/platform/context";
export default async function PlatformAdmin() { const ctx = await requireApplicationContext("PLATFORM_ADMIN"); const [{ count: organizations }, { count: users }, { count: invitations }] = await Promise.all([ctx.supabase.schema("platform").from("organizations").select("id", { count: "exact", head: true }), ctx.supabase.schema("iam").from("organization_memberships").select("id", { count: "exact", head: true }), ctx.supabase.schema("iam").from("invitations").select("id", { count: "exact", head: true }).eq("status", "pending")]); return <AdminShell kind="platform"><main className="workspace-main"><header className="re-page-header"><div><span className="re-eyebrow">Platform administration</span><h1>Platform dashboard</h1><p>Manage organizations, application access, invitations and platform governance.</p></div><div className="re-actions"><Link className="re-button primary" href="/platform/users/new">Invite director</Link><Link className="re-button secondary" href="/platform/provisioning">Provision customer</Link></div></header><div className="re-stats"><div className="re-stat"><span>Organizations</span><strong>{organizations ?? 0}</strong></div><div className="re-stat"><span>Users</span><strong>{users ?? 0}</strong></div><div className="re-stat"><span>Pending invitations</span><strong>{invitations ?? 0}</strong></div><div className="re-stat"><span>Access model</span><strong>Scoped</strong></div></div><section className="re-surface"><div className="re-section-head"><div><span className="re-eyebrow">Access operations</span><h2>Organization users and roles</h2><p>Every user record shows its organization, application assignment and role.</p></div><Link className="re-button secondary" href="/platform/users">View users</Link></div></section></main></AdminShell>; }
