import { ReHeader } from "@/modules/real-estate/components/Shell";
import { UnitForm } from "@/modules/real-estate/components/Forms";
import { listProperties, listUnits } from "@/modules/real-estate/repositories";
import { getRealEstateTenantContext } from "@/modules/real-estate/services/tenant-context";
export default async function NewUnit({searchParams}:{searchParams:Promise<{propertyId?:string}>}){const {propertyId}=await searchParams; const ctx=await getRealEstateTenantContext(); const [{data:propertyData},{data:unitData}]=await Promise.all([listProperties(ctx.supabase,ctx,"",{pageSize:1000,sort:"name"}),listUnits(ctx.supabase,ctx,"",{pageSize:1000})]); const properties=(propertyData??[]).map((property:any)=>({id:property.id,name:property.name,property_code:property.property_code})); const defaults=["single_room","studio","1BR","2BR","3BR","4BR","commercial","office","shop","residential","parking"]; const customUnitTypes=Array.from(new Set((unitData??[]).map((unit:any)=>unit.unit_type).filter((type:any)=>type && !defaults.includes(type)))); return <main className="re-main"><ReHeader eyebrow="Units" title="Add unit" description="Create an actual unit record. Planned counts remain planning data only."/><UnitForm propertyId={propertyId} properties={properties} customUnitTypes={customUnitTypes}/></main>}
