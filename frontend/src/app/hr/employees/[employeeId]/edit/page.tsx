import { notFound } from "next/navigation"; import { AppShell } from "@/components/AppShell"; import { requireApplicationContext } from "@/lib/platform/context"; import { EmployeeForm, HrTop, hrGroups } from "@/modules/hr/components";
export default async function EditEmployee({params}:{params:Promise<{employeeId:string}>}){const {employeeId}=await params; const ctx=await requireApplicationContext("HR"); const {data}=await ctx.supabase.schema("hr").from("employees").select("*").eq("id",employeeId).eq("organization_id",ctx.organizationId).maybeSingle(); if(!data)notFound(); return <AppShell app="HR" logo="/brands/hr/logo.jpg" alt="Hakika HR logo" groups={hrGroups}><HrTop title={`Edit ${data.display_name}`} description="Update employee identity and employment details."/><main className="workspace-main"><EmployeeForm employee={data}/></main></AppShell>}
