import Link from "next/link"; import { notFound } from "next/navigation"; import { AppShell } from "@/components/AppShell"; import { requireApplicationContext } from "@/lib/platform/context"; import { EmployeeAvatar, HrTop, hrGroups } from "@/modules/hr/components"; import type { Employee } from "@/modules/hr/types";
export default async function EmployeeDetails({params}:{params:Promise<{employeeId:string}>}){const {employeeId}=await params; const ctx=await requireApplicationContext("HR"); const {data}=await ctx.supabase.schema("hr").from("employees").select("id,organization_id,company_id,employee_number,first_name,middle_name,last_name,display_name,email,phone,department_id,designation_id,employment_type,employment_status,employment_start_date,probation_end_date,profile_image_url,pwd_status,portal_access_status,archived_at").eq("id",employeeId).eq("organization_id",ctx.organizationId).maybeSingle(); if(!data)notFound(); const employee=data as Employee; return <AppShell app="HR" logo="/brands/hr/logo.jpg" alt="Hakika HR logo" groups={hrGroups}><HrTop title={employee.display_name} description={`${employee.employee_number} · Employee profile`} action={<div className="employee-actions"><Link className="button secondary" href={`/hr/employees/${employee.id}/edit`}>Edit Employee</Link><Link className="button primary" href={`/hr/employees/${employee.id}/access`}>Manage Access</Link></div>}/><main className="workspace-main"><section className="card panel profile-hero"><EmployeeAvatar employee={employee}/><div><span className="context">EMPLOYEE PROFILE</span><h2>{employee.display_name}</h2><p>{employee.email||"No email"} · {employee.phone||"No phone"}</p></div></section><div className="profile-tabs"><section className="card panel"><h2>Overview</h2><dl><dt>Employee number</dt><dd>{employee.employee_number}</dd><dt>Status</dt><dd>{employee.employment_status}</dd><dt>Employment type</dt><dd>{employee.employment_type}</dd><dt>Start date</dt><dd>{employee.employment_start_date||"Not set"}</dd><dt>Business OS access</dt><dd>{employee.portal_access_status.replaceAll("_"," ")}</dd></dl></section><section className="card panel"><h2>Protected sections</h2><p className="context">Sensitive statutory, banking, compensation, medical and documents tabs are permission-gated and are not loaded into this general profile.</p><div className="employee-actions"><Link className="button secondary" href={`/hr/employees/${employee.id}/access`}>Application Access</Link></div></section></div></main></AppShell>}
