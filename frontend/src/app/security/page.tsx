import { PublicShell } from "@/components/PublicShell"; export default function Security(){return <PublicShell><main className="public-main"><div className="eyebrow">Security by design</div><h1>Your business stays in its lane.</h1><p className="section-intro">Hakika is structured around explicit organization boundaries, scoped company access, application entitlements and server-enforced permissions.</p><div className="feature-grid"><div className="feature"><h3>Tenant isolation</h3><p>Organizations are hard boundaries. Every company-owned query carries organization and company scope.</p></div><div className="feature"><h3>Auditable actions</h3><p>Meaningful business and security events are recorded without passwords, tokens or sensitive message bodies.</p></div><div className="feature"><h3>Least privilege</h3><p>Read-only roles are permission sets, not UI hints. RLS remains the final enforcement layer.</p></div></div></main></PublicShell>}
