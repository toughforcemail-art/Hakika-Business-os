const firstAction: Record<string, string> = { "Hakika Real Estate": "Add your first property", HR: "Invite your first teammate", Finance: "Set up your chart of accounts", "ToughForce Security": "Add your first guard", "Customer Admin": "Invite your first member", "Platform Admin": "Provision a customer organization" };

export function WorkspaceDashboard({ app, eyebrow }: { app: string; eyebrow: string }) {
  return <><div className="topbar"><div><span className="context">Secure workspace</span><h1>{eyebrow}</h1></div><div className="context">Live data · scoped access</div></div><main className="workspace-main"><div className="kpi-grid"><div className="card kpi"><div className="kpi-label">Records</div><div className="kpi-value">0</div><div className="kpi-meta">No records created yet</div></div><div className="card kpi"><div className="kpi-label">Team members</div><div className="kpi-value">0</div><div className="kpi-meta">No members to display</div></div><div className="card kpi"><div className="kpi-label">Tasks</div><div className="kpi-value">0</div><div className="kpi-meta">No open tasks</div></div><div className="card kpi"><div className="kpi-label">Entitlement</div><div className="kpi-value" style={{ fontSize: 20 }}>Entitled</div><div className="kpi-meta">{app} access</div></div></div><section className="card panel empty-dashboard"><div className="empty-mark">+</div><h2>Your {app} workspace is ready</h2><p>There is no sample data in this workspace. Add real records when you are ready and they will appear here within your organization scope.</p><button className="button primary" type="button">{firstAction[app] ?? "Start setup"}</button></section></main></>;
}
