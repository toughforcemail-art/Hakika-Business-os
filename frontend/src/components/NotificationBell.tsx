import Link from "next/link";

type NotificationPreview = { id: string; title: string; message: string; severity: string; action_url: string | null };
export function NotificationBell({ unreadCount, recent }: { unreadCount: number; recent: NotificationPreview[] }) { return <details className="notification-widget"><summary className="notification-bell" aria-label={`${unreadCount} unread notifications`}><span aria-hidden="true">!</span><span className="notification-bell-label">Notifications</span>{unreadCount > 0 && <b>{unreadCount > 99 ? "99+" : unreadCount}</b>}</summary><div className="notification-widget-panel"><div className="notification-widget-head"><strong>Notifications</strong><Link href="/notifications">View all</Link></div>{recent.length ? recent.map((item) => <article className="notification-widget-item" key={item.id}><span className={`notification-widget-dot severity-${item.severity}`} /><div><strong>{item.title}</strong><p>{item.message}</p>{item.action_url && <Link href={item.action_url}>Open</Link>}</div></article>) : <p className="notification-widget-empty">You are all caught up.</p>}</div></details>; }
