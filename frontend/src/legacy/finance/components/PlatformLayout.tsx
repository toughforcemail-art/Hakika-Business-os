// @ts-nocheck
import React from 'react';
import { ChevronRight, Menu, X } from 'lucide-react';
import { useLocation, useNavigate } from 'react-router-dom';
import PlatformCommandCenter from './PlatformCommandCenter';
import { platformApplications, platformModules } from '../platform/platformRegistry';
export { platformApplications, platformModules as platformNavigation } from '../platform/platformRegistry';

const PlatformLayout: React.FC<{ children: React.ReactNode; onLogout: () => void }> = ({ children, onLogout }) => {
  const location = useLocation(); const navigate = useNavigate(); const [open, setOpen] = React.useState(false);
  const active = platformModules.find((item) => location.pathname === item.route || (item.route !== '/app/platform-preview' && location.pathname.startsWith(`${item.route}/`)));
  const installed = platformApplications.filter((item) => item.status === 'INSTALLED'); const marketplace = platformApplications.filter((item) => item.status !== 'INSTALLED');
  const nav = (path: string) => { const application = platformApplications.find((item) => item.route === path); if (application) { const productionRoute = application.id === 'toughforce' ? '/app/security/dashboard' : (application.productionRoute || application.route); window.open(productionRoute, '_blank', 'noopener,noreferrer'); } else navigate(path); setOpen(false); };
  return <div className="flex min-h-screen bg-slate-100 text-slate-900 dark:bg-slate-950 dark:text-white"><aside className={`${open ? 'translate-x-0' : '-translate-x-full'} fixed inset-y-0 left-0 z-40 w-72 overflow-y-auto border-r border-slate-200 bg-white p-4 transition-transform dark:border-white/10 dark:bg-slate-900 lg:static lg:translate-x-0`}><div className="mb-6 flex items-center justify-between"><button onClick={() => nav('/app/platform-preview')} className="text-left"><p className="text-xs font-bold uppercase tracking-[.2em] text-indigo-500">Hakika</p><p className="text-lg font-bold">Platform Preview</p></button><button className="lg:hidden" onClick={() => setOpen(false)}><X size={18} /></button></div><p className="mb-2 px-3 text-[10px] font-bold uppercase tracking-[.18em] text-slate-400">Platform</p><nav className="space-y-1">{platformModules.map(({ id, title, icon: Icon, route, badge }) => <button key={id} onClick={() => nav(route)} className={`flex w-full items-center justify-between rounded-lg px-3 py-2 text-left text-sm font-semibold ${active?.id === id ? 'bg-indigo-600 text-white' : 'text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-white/10'}`}><span className="flex items-center gap-3"><Icon size={16} />{title}</span>{badge && <span className="text-[10px] opacity-70">{badge}</span>}</button>)}</nav><p className="mb-2 mt-7 px-3 text-[10px] font-bold uppercase tracking-[.18em] text-slate-400">Applications · Installed</p><nav className="space-y-1">{installed.map(({ id, name, icon: Icon, route }) => <button key={id} onClick={() => nav(route)} className="flex w-full items-center gap-3 rounded-lg px-3 py-2 text-left text-sm text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-white/10"><Icon size={15} />{name}</button>)}</nav><p className="mb-2 mt-5 px-3 text-[10px] font-bold uppercase tracking-[.18em] text-slate-400">Applications · Marketplace <span className="normal-case">({marketplace.length} available)</span></p><nav className="space-y-1">{marketplace.map(({ id, name, icon: Icon, route }) => <button key={id} onClick={() => nav(route)} className="flex w-full items-center justify-between rounded-lg px-3 py-2 text-left text-sm text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-white/10"><span className="flex items-center gap-3"><Icon size={15} />{name}</span><span className="text-[10px] text-slate-400">Coming soon</span></button>)}</nav><button onClick={onLogout} className="mt-8 w-full rounded-lg border border-slate-200 px-3 py-2 text-left text-sm text-slate-500 dark:border-white/10">Exit preview</button></aside><div className="min-w-0 flex-1"><header className="sticky top-0 z-30 flex h-16 items-center justify-between border-b border-slate-200 bg-white/90 px-4 backdrop-blur dark:border-white/10 dark:bg-slate-900/90"><div className="flex items-center gap-3"><button className="lg:hidden" onClick={() => setOpen(true)}><Menu size={20} /></button><div className="flex items-center gap-2 text-sm text-slate-500"><span>Platform</span><ChevronRight size={14} /><span className="font-semibold text-slate-900 dark:text-white">{active?.title || 'Application Preview'}</span></div></div><div className="flex items-center gap-3"><PlatformCommandCenter /><span className="rounded-full border border-amber-200 bg-amber-50 px-3 py-1 text-xs font-bold text-amber-700">PREVIEW · READ ONLY</span></div></header><main>{children}</main></div></div>;
};
export default PlatformLayout;
