export function PageHeader({ eyebrow, title, description, actions }: { eyebrow: string; title: string; description?: string; actions?: React.ReactNode }) { return <header className="re-page-header"><div><span className="re-eyebrow">{eyebrow}</span><h1>{title}</h1>{description && <p>{description}</p>}</div>{actions && <div className="re-page-actions">{actions}</div>}</header>; }
export function EmptyState({ title, description }: { title: string; description: string }) { return <section className="re-surface re-empty-state"><div className="re-empty-mark">+</div><h2>{title}</h2><p>{description}</p><a className="button" href="/support">Request setup help</a></section>; }
export function StatusBadge({ children }: { children: React.ReactNode }) { return <span className="re-status-badge">{children}</span>; }
export function PermissionBoundary({ allowed, children, fallback = null }: { allowed: boolean; children: React.ReactNode; fallback?: React.ReactNode }) { return allowed ? children : fallback; }
