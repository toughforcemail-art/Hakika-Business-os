# Terminology

- Organization: mandatory tenant boundary.
- Application: a Hakika product.
- Company: optional subdivision inside an organization.
- Workspace: a UI concept, never a universal security boundary.
- Platform owner: the single organization marked `platform_owner`; it is verified from the database and is not duplicated during provisioning.
