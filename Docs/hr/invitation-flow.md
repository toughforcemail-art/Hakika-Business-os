# Invitation flow

Saving an employee does not create an Auth user or password. The access screen prepares a separate invitation using the existing hashed-token, expiry, membership, role, and audit flow. Delivery remains a separate operation and is not sent by ordinary employee creation.
