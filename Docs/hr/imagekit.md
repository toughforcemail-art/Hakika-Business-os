# ImageKit

The shared protected ImageKit authentication endpoint remains server-only. HR profile/document uploads must use `hakika/{organizationId}/hr/employees/{employeeId}/profile/` or `/documents/`; browser folders are never trusted. No file was uploaded during this implementation.
