# Shared packages

Reserved for shared UI, auth, tenancy, permissions, database, validation, config and observability contracts. Application packages must not import browser-only code into backend services.
